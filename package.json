{
  "name": "rockys-retreat-and-rambles",
  "version": "0.1.0",
  "private": true,
  "description": "Rocky's Retreat and Rambles — Next.js app (frontend lives in /frontend).",
  "scripts": {
    "postinstall": "cd frontend && yarn install --frozen-lockfile=false",
    "build": "cd frontend && yarn build",
    "dev": "cd frontend && yarn dev",
    "start": "cd frontend && yarn start"
  },
  "dependencies": {
    "next": "^16.2.4"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "yarn@1.22.22"
}
